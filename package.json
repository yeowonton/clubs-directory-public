{
  "name": "clubs-directory",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20",
    "npm": ">=10"
  },
  "scripts": {
    "dev": "concurrently -k -n \"server,ui\" -c \"cyan,magenta\" \"nodemon --watch server.js --signal SIGTERM server.js\" \"browser-sync start --proxy http://localhost:3000 --files public --port 5173 --no-ui --no-notify\"",
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.1",
    "express": "^5.1.0",
    "mysql2": "^3.14.3"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10"
  }
}
