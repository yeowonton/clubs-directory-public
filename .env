# --- Server ---
PORT=3000
NODE_ENV=development

# --- Admin (commissioner) login ---
# The admin page accepts either the raw code or its SHA-256 hash (from the UI).
ADMIN_CODE=commissioner2025

# --- President submission gate ---
# This is the single password that allows presidents to submit the form.
PRESIDENT_PASSWORD=devpres2025

# --- MySQL ---
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=clubs_db

# --- Rate limiting (5 attempts per 10 minutes by default) ---
RATE_LIMIT_MAX_ATTEMPTS=5
RATE_LIMIT_WINDOW_MS=600000
